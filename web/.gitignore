# Dependencies
node_modules/

# Build output
dist/

# Electron release artifacts
release/

# Secrets
.env
.env.local
.env.*.local

# Electron credential files (auto-generated, contain real secrets)
electron/credentials.json
electron/credentials.mjs

# Persisted jInvoice secret (set by user in Settings, contains secret key)
jinvoice-secret.json

# Vite cache
.vite/

# Local invoice database export (contains real personal financial data)
cloud-db.json

# Mobile relay server (not ready for production)
server/mobile.mjs
