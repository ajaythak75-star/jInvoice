<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required for Gmail / Microsoft Graph API calls and WorkManager network check -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- WorkManager restarts periodic polling after device reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Google Account access for Gmail OAuth -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

    <!-- MediaStore scoped storage — Downloads/jInvoice/ only; no broad storage permission needed on API 29+ -->

    <application
        android:name=".JInvoiceApp"
        android:label="@string/app_name"
        android:theme="@style/Theme.jInvoice.NoActionBar"
        android:allowBackup="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.autoimport.ConsentActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- MSAL redirect URI handler for Outlook OAuth -->
        <activity
            android:name="com.microsoft.identity.client.BrowserTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Replace with your actual redirect URI scheme from msal_auth_config.json -->
                <data
                    android:host="com.jinvoice"
                    android:path="/YOUR_BASE64_URL_ENCODED_PACKAGE_SIGNATURE"
                    android:scheme="msauth" />
            </intent-filter>
        </activity>

        <!-- WorkManager auto-initialisation provider is included transitively via work-runtime -->

    </application>

</manifest>
